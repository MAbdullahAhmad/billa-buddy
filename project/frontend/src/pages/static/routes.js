// src/pages/static/routes.js

import Homepage from './Homepage/Homepage';

const routes = [
  {
    name: 'home',
    path: '/',
    component: Homepage,
    exact: true
  },
];

export default routes;